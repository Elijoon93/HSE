# آزمون پذیرش v4.1

## برند
1. سطح هلدینگ «فولاد یزد» نمایش داده شود.
2. سطح شرکت عملیاتی «SISOO / جنوب شرق ایرانیان» نمایش داده شود.
3. برای کانسار هامون برند مستقل نمایش داده شود.
4. تغییر تم، لوگوهای قفل‌شده را حذف یا جایگزین نکند.

## Pending preview
1. درخواست عادی ثبت شود.
2. برنامه وارد حالت پیش‌نمایش شود.
3. همه منوها دیده شوند.
4. کلیک روی ماژول عملیاتی پیام قفل بدهد.
5. Records و Actions هیچ داده واقعی نمایش ندهند.
6. دکمه فعال‌سازی، پنل Pending را باز کند.

## تم
تمام تم‌ها بررسی شوند:
Corporate Locked, Enterprise Navy, Mining Bronze, Factory Steel,
Academic Blue, High Contrast, System Theme.

## کنترل دستگاه
1. Clear Local Records حساب و تم را حفظ کند.
2. Reset Device فقط بعد از تایپ RESET انجام شود.
3. خروج از حساب رکوردهای محلی را حذف نکند.

## مدیر کل اختصاصی
1. درخواست با نام دکتر میثم کوهساری ثبت شود.
2. کلید صحیح شش‌رقمی وارد شود.
3. حساب با UserCode=USR-ROOT-002 و SystemOwner فعال شود.
4. همان کلید برای درخواست نام دیگر رد شود.
5. همان کلید روی دستگاه دیگر پس از Binding رد شود.
6. پنج خطای متوالی، قفل ۳۰ دقیقه‌ای ایجاد کند.
